4. Bài phân tích lý thuyết: Tại sao không dùng double/float cho bài toán tài chính?4.1. Bản chất chuẩn biểu diễn số thực dấu phẩy động IEEE 754Trong máy tính, các kiểu dữ liệu nguyên thủy float (32-bit) và double (64-bit) lưu trữ số thực dưới dạng nhị phân theo chuẩn IEEE 754 (gồm bit dấu, số mũ exponent và phần định trị mantissa).Trong hệ thập phân (cơ số 10), các phân số như $0.1$ ($\frac{1}{10}$) hoặc $0.075$ ($\frac{75}{1000}$) là số thập phân hữu hạn. Tuy nhiên, khi chuyển sang hệ nhị phân (cơ số 2), chúng trở thành dãy số vô hạn tuần hoàn.4.2. Hiện tượng trôi sai số (Floating-point Imprecision)Do giới hạn bộ nhớ 32-bit hoặc 64-bit, CPU buộc phải cắt tỉa và làm tròn các bit cuối cùng. Điều này dẫn đến sai số ngay khi gán giá trị:Javadouble a = 0.1;
   double b = 0.2;
   System.out.println(a + b); // Kết quả: 0.30000000000000004 thay vì 0.3
   4.3. Hậu quả trong hệ thống tài chính & tính phí Token LLMLệch sổ sách và thất thoát tài chính: Giá của Token LLM rất nhỏ (ví dụ $\$0.000000075$ cho 1 token). Khi nhân với hàng tỷ lượt token tiêu thụ mỗi tháng trên toàn hệ thống RikkeiPay, các sai số tích lũy ở phần đuôi nhị phân sẽ biến thành số tiền chênh lệch đáng kể giữa báo cáo nội bộ và hóa đơn nhà cung cấp.Sai sót điều kiện so sánh và kiểm toán: Trong lập trình tài chính, phép so sánh if (cost1 == cost2) dùng double hầu như sẽ trả về false do sai số ở các chữ số thập phân xa.Giải pháp với BigDecimal: BigDecimal trong Java lưu trữ số thực dưới dạng số nguyên không giới hạn chiều dài (BigInteger unscaledValue) và một số nguyên chỉ vị trí dấu phẩy (int scale). Nó biểu diễn chính xác tuyệt đối các con số thập phân và cho phép chỉ định rõ ràng chế độ làm tròn (RoundingMode.HALF_UP), đáp ứng nghiêm ngặt tiêu chuẩn kế toán và kiểm toán ngân hàng.5. Minh chứng chạy thực tế (Console Log Unit Test Pass 100%)
5. > Task :compileJava UP-TO-DATE
> Task :processResources UP-TO-DATE
> Task :classes UP-TO-DATE
> Task :compileTestJava
> Task :processTestResources NO-SOURCE
> Task :testClasses
> Task :test
BUILD SUCCESSFUL in 1s
4 actionable tasks: 2 executed, 2 up-to-date
Consider enabling configuration cache to speed up this build: https://docs.gradle.org/9.5.1/userguide/configuration_cache_enabling.html
2:06:11 PM: Execution finished ':test --tests "com.pe.ss11b1.assistant.LlmCostCalculatorTest"'.
